**How to start:**
```
chmod +x apps.sh
./apps.sh start
```
This will create an `instance.lock` file.

**How to stop:**
```
./apps.sh stop
```

Logs will be appended to `logs/[servicename].out` file.